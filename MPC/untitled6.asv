A = s;
idx = find(mod(1:length(A), 6) == 3 | mod(1:length(A), 6) == 1 | mod(1:length(A), 6) == 2);
A = flip(A(idx));
B = [];
%A = 1:100;
for i = 1:3:length(A)
    sub_vector = A(i:min(i+2, length(A)));
    disp(sub_vector)
    disp('fgs')
    B = [B; flip(sub_vector)];
end
B= B